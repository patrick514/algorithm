function lengthOfLIS(nums: number[]): number {
    let ans
    let p:number[] = []
    for()
    return ans
}

function binarySearch(num,nums):number{
    let index

    let left = 0
    let right = nums.length -1
    
    while(left <= right){
        let mid = Math.floor((right - left)/2) + left
        if(num )
    }

    return index
}
